import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#eb6f4c',
    },
    secondary: {
      main: '#eb6f4c',
    },
    text: {
      primary: '#ffffff',
      secondary: '#ffffff'
    },
  },
}
);

export default Theme